
<style>
    .btnn1:hover{
        transform: scale(1.03);
    }
</style>


<form method="post" action="{{ path('app_offres_delete', {'idOffre': offre.idOffre}) }}" onsubmit="return confirm('Are you sure you want to delete this Offre?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.idOffre) }}">
    <button class="button fullwidth_block margin-top-5 btnn1"> </i>Delete</button>
</form>
