<form method="post" action="{{ path('app_chat_delete', {'idChat': chat.idChat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chat.idChat) }}">
    <button style="background-color: transparent;border-color: transparent"><i class="fa fa-close" style="color:red"></i></button>

</form>
